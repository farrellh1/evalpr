diff --git a/src/users.js b/src/users.js
index 9a1b2c3..4d5e6f7 100644
--- a/src/users.js
+++ b/src/users.js
@@ -1,13 +1,13 @@
 const { db } = require('./database');

-const tableName = 'users';
+const USER_TABLE = 'users';

-function getUser(id) {
-  return db.query('SELECT * FROM ' + tableName + ' WHERE id = $1', [id]);
+function findUserById(id) {
+  return db.query('SELECT * FROM ' + USER_TABLE + ' WHERE id = $1', [id]);
 }

 function listUsers() {
-  return db.query('SELECT * FROM ' + tableName);
+  return db.query('SELECT * FROM ' + USER_TABLE);
 }

 module.exports = { findUserById, listUsers };
